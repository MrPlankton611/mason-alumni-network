git add . and commit every 50-100 lines of code or at a logical milestone. Also do not add yourself as a coauthor at all.

Page HTML source of truth is `alumni-src/pages/` + `alumni-src/partials/`. `alumni/*.html` are generated by `scripts/build.js` and gitignored — never edit them directly; edit the source and run `node scripts/build.js` (or `npm start`, which builds automatically via a `prestart` hook).
